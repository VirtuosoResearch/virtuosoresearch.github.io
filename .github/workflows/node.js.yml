name: Node.js CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: Use Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20.x'
      - run: npm install
      - run: npm install --save-dev @babel/plugin-proposal-private-property-in-object
      - run: CI=false npm run build --if-present
      - run: npm install --save-dev gh-pages   
      - name: Set Git user identity
        run: |
          git config --global user.email "hongyang90@gmail.com"
          git config --global user.name "hongyangsg"
          git remote set-url origin https://hongyangsg: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@github.com/VirtuosoResearch/virtuosoresearch.github.io
      - run: CI=false npm run deploy
